server:
  port: 8090
spring:
  datasource:
    url: jdbc:h2:tcp://localhost/~/test
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        # show_sql: true
        format_sql: true

  security:
    user:
      name: "camport"
      password: "camport"

logging.level:
  org.hibernate.SQL: debug
# org.hibernate.type: trace # binding parameter 확인